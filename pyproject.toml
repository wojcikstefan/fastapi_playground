[project]
name = "fastapi-playground"
version = "0.1.0"
description = "Just a place for me to have fun with FastAPI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.129.2",
]
